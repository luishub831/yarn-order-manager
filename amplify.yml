version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm run bild
        - npm ci
    build:
      commands:
        - npx --yes nuxt@3.11.2 build
  artifacts:
    baseDirectory: .output/public
    files:
      - '**/*'